## Issue と PR の運用ルール

### Issue と AGENTS.md の優先順位

- Issue は対象スコープ、背景、目的、完了条件を定義する
- AGENTS.md は実装、テスト、PR、マージの運用条件を定義する
- Issue の解釈と AGENTS.md の運用条件が競合する場合は、AGENTS.md の条件を優先する
- 実装着手前に、Issue の完了条件と AGENTS.md 上の必須条件を両方満たす進め方になっていることを確認する

### 判断ミスが起きたときの扱い

- 実装後に判断ミスや運用漏れが見つかった場合は、その場の修正だけで終わらせず、原因を短く言語化する
- 原因が再発しうる判断基準の不足、優先順位の曖昧さ、完了条件の抜けである場合は、AGENTS.md の該当箇所にルールを追記して再発防止までを作業に含める
- 追記するルールは、そのミスを次回の着手前または完了前に検知できる内容にする
- AGENTS.md への追記が必要な変更を行った場合は、なぜ追加したのかをコミットや PR 説明でも分かるように残す

### Issue 作成基準

**作成する**

- 仕様追加、仕様変更、設計判断など、実装前に合意や記録が必要なとき
- バグ、不具合、期待との差分があり、再現条件や影響範囲を追跡したいとき
- ドキュメント更新、調査、改善タスクなど、作業単位として残した方がよいとき
- 1 回のコミットや会話では完結せず、複数ステップで進める見込みがあるとき

**作成しない**

- その場で直せる軽微な誤字、表記ゆれ、コメント修正のみのとき
- 既存 Issue の作業範囲に明確に含まれており、別管理にすると重複するだけのとき
- 一時的な調査メモで終わり、判断やアクションに接続しないとき

### Issue に最低限書くこと

- 背景
- 目的
- スコープ
- 完了条件
- 関連資料: PR、Docs、ADR、Figma など
- ラベル: 少なくとも 1 つは機能ラベルを付ける
- 見積りや優先度に影響する前提があれば本文に明記し、1 Issue 1 テーマを基本とする
- Issue 本文そのものをリポジトリ内の Markdown として管理する必要はない。判断や仕様として残すべき内容だけを Docs や ADR に反映する
- GitHub への Issue 追加は `gh` コマンドを使って自動で行う
- `gh` コマンドで Issue を追加する前に、タイトル、本文、ラベル、`blocked` の有無をユーザーに確認する
- Issue の依存関係追加、確認は `gh api` を優先して使う
- `gh api` で依存関係を扱うときは Issue 番号ではなく GitHub の内部 `issue_id` を使う
- `blocked` を本文に記載するだけで済ませず、可能な限り GitHub の依存関係にも反映する

### Issue ラベルとリレーションのルール

- Issue 作成時は、対象機能を表すラベルを必ず付ける
- 機能横断の課題でも、主たる影響先となる機能ラベルを 1 つは付ける
- `message` ラベルは特殊な意味を持つため、機能ラベルとして使わない
- 機能ラベルは定義済みの一覧から選び、同じ意味の別名を増やさない
- 新しい機能ラベルを追加した場合は、`AGENTS.md` の定義済みラベル一覧にも追記する
- 他 Issue の完了待ち、仕様未確定、外部確認待ちなどで着手できない場合は、`blocked` のリレーションを設定する
- `blocked` を設定するときは、本文に解消条件を明記する
- 解消後はリレーションを更新し、依存関係がわかる状態を保つ
- 単に優先度が低いだけの Issue には使わない

### Issue タイトルとコミットメッセージのルール

- Issue タイトルは、課題や目的がそのまま伝わる自然な日本語にする
- 機能名はタイトル先頭に必須ではなく、機能ラベルで識別できればよい
- 実装手段ではなく、課題や目的がわかる表現を使い、`作成` `修正` `対応` だけで終わる曖昧な題名を避ける
- 例: `週表示で月またぎの日付を選ぶと別日の予定詳細を開いてしまう`
- コミットメッセージは日本語で書き、適切なタイミングで自動的にコミットする
- 1 コミット 1 論点を基本とし、形式は `type: 内容` を基本とする
- `fix`, `feat`, `docs`, `refactor`, `test`, `chore` などの種別を使い分ける
- 例: `fix: 週表示で月またぎの日付が別日の詳細を開く問題を修正`

### PR とマージのルール

- PR には、目的、主な変更点、確認してほしい観点を明記する
- レビューでは、仕様との整合、回帰有無、影響範囲、テスト有無を優先して確認する
- UI 変更を含む場合は、画面確認結果やスクリーンショットを付ける
- GitHub の自動紐づけを前提とし、`1 Issue = 1 PR`、PR への関連 Issue 明記は必須にしない
- 品質は過度に求めすぎず、現時点で目的を達成できることを優先する
- ただし、致命的な不具合、明確な仕様違反、必要な E2E テスト未整備の PR はマージしない
- PR 作成前、またはレビュー依頼前までに `npm run ci` を実行し、成功していることを確認する
- ローカル環境固有の要因で `npm run ci` を完走できない場合は、失敗箇所、理由、CI 上で確認すべき点を PR に明記する
- 配布物や build workflow を変更する場合は、OAuth 設定や秘密情報が成果物に含まれないことを確認し、可能な限り workflow やテストで自動検知する
- 既知の不足や後続改善がある場合は、見落とさず Issue として残す
- 現在の PR の目的に含まれる軽微な修正はその場で対応し、別論点、広範囲、追加設計が必要なものは Issue に切り分ける
- 切り分けた場合は、PR 上で後続対応であることがわかるようにコメントを残す
- マージ前に Docs の更新漏れがないか確認し、仕様判断を含む場合は `Docs/` 配下の関連文書も更新対象として示す
- Issue クローズは PR との自動紐づけによる運用を前提とする

### テスト作成のルール

- 実装や修正を行った場合は、影響範囲に応じて必要なテストを追加または更新する
- 原則として、作成した機能は少なくとも 1 本以上の E2E テストで代表フローを保証する
- 同一フロー上の差分やバリエーションに近い部分は、結合テストでの保証も許容する
- E2E で一度も通していない新規フローは、原則としてマージ対象にしない
- バグ修正では、可能な限り再発防止になるテストを追加する
- unit テストで十分に確認できる処理は unit テストを補助的に使う
- 結合テストでは、可能な限りモックを減らし、本番に近い組み合わせで検証する
- 外部 API が必要な内容は、原則として Prism を使って検証する
- テスト追加が難しい場合は、理由と確認方法を PR に明記する

### モックとフォールバックのルール

- モックやフォールバックを使うかどうかは、実装前に意図を明確にして判断する
- 外部 API の代替や検証は、原則として Prism を使い、外部 API 向けの単純なモックは新規に増やさない
- モック利用は、Prism では代替しにくい UI 仮表示や仕様未確定部分に限り、どこが仮実装かがコード、PR、UI からわかる状態にする
- フォールバックは、本実装が失敗した場合でも最低限の利用継続が必要な箇所に限って使い、常時使われる前提や実質的なメインルートになる設計は認めない
- フォールバックは障害を隠蔽せず、データ不整合や欠陥の恒久的な逃げ道として使わない。失敗は失敗として扱い、ログ、メトリクス、ユーザー通知などで検知可能にする
- モックやフォールバックを採用した場合は、本実装への置き換えや解消条件を Issue として残す
- メインパスより複雑なフォールバックを作る場合は、同等以上のテストを用意する

### reviewのルール

- レビュー結果は日本語で回答すること。
- バグ、回帰、不足しているテスト、セキュリティリスク、保守性の問題を優先してください。
- 指摘は簡潔にし、問題点・影響・修正案を含めてください。
- 単なる好みや軽微なスタイル差分だけでは指摘しないでください。
- 挙動変更がある場合は、対応するテストが追加・更新されているか確認してください。
- secrets、token、個人情報、認証情報がログ出力されていないか確認してください。
- 可能な限り、コードの意図や背景を理解してからレビューしてください。
- 無理に完璧なコードを求めすぎず、現時点で目的を達成できることを優先してください。
- レビューした結果、不具合等がない場合、その旨をコメントしてください。
